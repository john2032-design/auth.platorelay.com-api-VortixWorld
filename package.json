{
  "name": "platorelay-api",
  "version": "1.0.0",
  "description": "API for auth.platorelay.com bypass",
  "main": "api/platorelay.js",  // Adjust to your file name/path
  "scripts": {
    "start": "node api/platorelay.js",  // For local testing
    "build": "npm install"  // Optional: Custom build if needed for native modules
  },
  "dependencies": {
    "axios": "^1.7.2",  // For HTTP requests
    "opencv4nodejs": "^5.0.0"  // For CV/shape analysis (native module; check latest version)
  },
  "engines": {
    "node": ">=18.0.0"  // Vercel supports up to Node 20; use a recent version for security
  },
  "keywords": ["vercel", "serverless", "api"],
  "author": "Brad",
  "license": "ISC"
}
